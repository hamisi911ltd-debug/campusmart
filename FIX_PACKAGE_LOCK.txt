🔧 FIX PACKAGE-LOCK.JSON ISSUE
================================

CURRENT SITUATION:
------------------
Railway keeps failing with "npm ci" because package-lock.json is out of sync.
We've added nixpacks.toml to force "npm install", but if that doesn't work,
here's the manual fix:

MANUAL FIX (Run these commands):
---------------------------------
1. Open Command Prompt (NOT PowerShell - it has script execution disabled)

2. Navigate to backend folder:
   cd "C:\Users\Admin\OneDrive - Equity Bank (Kenya) Limited~\Desktop\SCHLP DOCS\my codes\campus-mart\backend"

3. Delete the old lock file:
   del package-lock.json

4. Regenerate it:
   npm install

5. Go back to root and commit:
   cd ..
   git add backend/package-lock.json
   git commit -m "Regenerate package-lock.json to fix Railway build"
   git push origin main

ALTERNATIVE: Use Git Bash
--------------------------
If you have Git Bash installed:

1. Right-click in the backend folder
2. Select "Git Bash Here"
3. Run:
   rm package-lock.json
   npm install
   cd ..
   git add backend/package-lock.json
   git commit -m "Regenerate package-lock.json"
   git push origin main

WHAT'S HAPPENING NOW:
---------------------
Railway is deploying with the nixpacks.toml fix (commit 0d43c17).
This should force "npm install" instead of "npm ci".

Check Railway dashboard in 2-3 minutes to see if it works.

IF NIXPACKS.TOML WORKS:
-----------------------
You'll see in Railway logs:
✅ "npm install" (not "npm ci")
✅ No "Missing: mysql2" errors
✅ Build completes successfully

IF IT STILL FAILS:
------------------
Run the manual fix above to regenerate package-lock.json properly.

COMMITS PUSHED SO FAR:
----------------------
1. 27ab6c2 - SSL fix in db.js
2. 3131ac5 - Updated railway.json (didn't work)
3. 0d43c17 - Added nixpacks.toml (current attempt)
